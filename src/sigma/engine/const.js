/** Define constants relating to the
 * usage of sigma and the parsing of its outputs */

// Environment variables
const environment = {
    source: 'SIGMA_SRC',
    home: 'SIGMA_HOME',
    git: 'ONTOLOGYPORTAL_GIT'
};

module.exports = {
    environment
};